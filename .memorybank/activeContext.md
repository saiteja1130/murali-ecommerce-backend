# Active Context (murali-ecommerce-backend)

## 1. Current State
- Server actively running on `http://localhost:5000`.
- MongoDB connected.
- Static file serving active on `/uploads`.
- REST endpoints for products, main-categories, categories, auth, and cart are active.

## 2. Recent Updates
- Maintained static image serving on `/uploads` ensuring customer storefront (port 3000) and admin (port 5173) can access media assets.
- Added `.github/copilot-instructions.md` and `.memorybank/` documentation.
